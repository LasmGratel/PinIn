[package]
name = "pinin"
version = "0.1.0"
edition = "2021"

[dependencies]
smallvec = { version = "1", features = ["union"] }
compact_str = "0.8"

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }

[[bench]]
name = "pinin_benchmark"
harness = false
